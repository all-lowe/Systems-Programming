Input Parameters:

    "cat <filename.csv> | ./sorter -c <column name to sort by>    >   <output directory>"
    
Description of Code: given a CSV file and a column name to sort by, this code will sort by the given column name on that file and return
                     sorted directory with the sorted contents.
